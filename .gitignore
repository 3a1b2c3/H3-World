# ── 大件：一律不入库 ─────────────────────────────────────────────
# 第三方框架 + 134 GB 权重。改动以 code/diffsynth_h3_action.patch 的形式提交，
# 复现方式见 README「复现」一节。
DiffSynth-Studio-h3/

# 视频切片 38.5 GB + metadata（最大单文件 34 MB）。由 code/abot/build_abot_clips.py 生成。
data/

# 训练产出：latent 缓存 68 GB、LoRA checkpoint、推理视频
output/

# 原始 bundle 解压件，只读留档
bundle/

# 环境与运行时缓存（HF / torch / triton / inductor 等，全部钉在 nvme）
.cache/
logs/

# 前两次失败方案（加性 bias / FiLM）的脚本。结论和诊断数字都记在 docs/journey.md
# 第一节，代码本身没有复现价值，留在本地备查即可。
code/abot/legacy/

# ── 常规 ─────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
*.tmp
*.tmp.*
*.rej
*.orig
.DS_Store

# ── 可视化页：内嵌视频的不入库 ────────────────────────────────
# 这几页每个 3–6 MB，内容是 base64 的 mp4。它们是**生成物**，而且每重建一次
# 就往 git 历史里塞一个全新的 5 MB blob，很快就会把仓库撑坏。
# 生成它们的脚本在 code/abot/build_*_viz.py，产出也发布成了 Artifact（见 docs/README.md）。
docs/action_prompt_viz.html
docs/action_prompt_text_viz.html
docs/action_ab_viz.html
docs/longvid_viz.html
# 例外：架构图页是纯 SVG、35 KB、无外部依赖，入库。
!docs/action_injection_arch.html
