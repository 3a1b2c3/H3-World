# Pinned from the working environment (CUDA 12.8).
# torch is installed separately from the cu128 index — see README.
torchvision==0.25.0
accelerate==1.14.0
transformers==4.57.3
safetensors==0.7.0
diffusers==0.37.1
numpy==2.2.6
pillow==12.3.0
imageio==2.37.4
imageio-ffmpeg==0.6.0
av==17.1.0
einops==0.8.2
sentencepiece==0.2.2
tqdm==4.69.1
ftfy==6.3.1
tensorboard==2.21.0
modelscope==1.39.1
huggingface-hub==0.36.2
triton==3.6.0
peft==0.20.0
# Not in the README's pinned list, but DiffSynth-Studio-h3-v2's
# diffsynth/core/__init__.py unconditionally imports unified_dataset.py,
# which imports pandas -- needed even for inference-only use, since Python
# requires the whole import chain to succeed regardless of what's actually
# called. No specific version verified compatible; unpinned since nothing
# else here depends on a particular pandas API.
pandas
